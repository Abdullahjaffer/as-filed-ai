# @filing-desk/web

React 19 + Vite + TypeScript + Ant Design 6 + Ant Design X.

Screens: Overview, Company (financial strip + chat + trace + filings), Compare, Changes, Evals.

Vite proxies `/api` → `http://localhost:4000`.

```bash
pnpm --filter @filing-desk/web dev
```
